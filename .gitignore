.env
.env.*
!.env.example
exploration/raw_breeds.json
__pycache__/
*.pyc
*.duckdb
*.duckdb.wal

# dbt
target/
dbt_packages/
logs/

# M9 observability — the Elementary report and the edr CLI's working dir are GENERATED,
# so regenerable, not committed (scripts/observability_report.sh) — same rule as the dbt
# docs lineage graph (DECISIONS §5: the laptop regenerates the demo, it isn't checked in).
# The permanent, reviewable change is the package wiring: dbt/packages.yml +
# dbt/package-lock.yml + the elementary block in dbt_project.yml — those ARE committed.
edr_target/
elementary_report.html
.venv-edr/
# dbt writes a per-machine anonymous id here — local state, not project config
.user.yml

.DS_Store
.venv/

# Heyra's case-study brief — their document, not mine to redistribute.
# Deliberately NOT a *.pdf glob: M6 exports the dashboard narrative as a PDF, which ships.
Heyra_Data_Engineer_Case.pdf

# My personal prep/memory aid — debrief rehearsal notes, not a project deliverable.
RECAP_NOTES.md

# The full reasoning long-form — the working draft every decision is argued out in.
# DECISIONS.md is the one-page deliverable curated FROM this; the draft stays local.
decisions_draft.md

# Personal dbt study guide (LaTeX + PDF) — local only, not a project deliverable
dbt_course/

# Personal debrief rehearsal prep (LaTeX + PDF) — local only, not a project deliverable
debrief_prep/
